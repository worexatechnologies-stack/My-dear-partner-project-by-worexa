[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = apps
addopts = -p no:cacheprovider --basetemp=pytest_temp
markers =
    postgresql: requires the opt-in PostgreSQL integration database
